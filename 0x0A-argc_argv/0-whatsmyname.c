#include <stdio.h>

/**
 * main - start of program
 * @argc: argument count
 * @argv: argument array or vector
 * Return: Always 0
 **/

int main(__attribute((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);

}
