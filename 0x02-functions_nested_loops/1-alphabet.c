#include "main.h"

/**
 * print_alphabet - prints the alphabet
 * main - start of the code
 * Return: Always zero
 **/
void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{		_putchar(i);
	}
}
