#include <stdio.h>

/**
 * main - start of the program
 * Return: zero for success
 **/

int main(void)
{
	int num;

	for (num = 0 ; num <= 9 ; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
